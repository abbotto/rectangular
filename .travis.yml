language: node_js

node_js:
  - "6.10"

branches:
  only:
    - master

script:
  - node_modules/eslint/bin/eslint.js --fix .
  - ~/.nvm/nvm.sh && cd install && chmod +x demo && ./demo && cd demo && npm run test
