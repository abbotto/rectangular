'use strict';

/**
 * Karma config.
 */
module.exports = function(config) {
    config.set({
        frameworks: [
            'mocha',
            'chai',
            'commonjs'
        ],

        reporters: [
            'mocha'
        ],

        mochaReporter: {
            showDiff: true
        },

        files: [
            'lib/*.js',
            'test/data/*.js',
            'test/helpers/*.js',
            'test/temp/*.js',
            'test/html-to-dom-client.js'
        ],

        exclude: [
            'lib/html-to-dom-server.js'
        ],

        browsers: [
            'PhantomJS',
            'Chrome'
        ],

        preprocessors: {
            'lib/**/*.js': ['commonjs'],
            'test/**/*.js': ['commonjs']
        }
    });
};
