"use strict";

/**
* Service - `upload$`
* - A service for uploading files
* - Requires `ng-file-upload`
*
* @module Extension->Upload-Service
*/

export default function ngFileUploadVendorService(Upload) {
	return Upload;
};
