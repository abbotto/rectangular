"use strict";

/**
* Service - `_`
* - A service for working with Objects, Arrays and Strings
* - Requires `lodash`
*
* @module Extension->_-Service
*/

export default function lodashVendorService($window) {
	return $window._;
};
